# Summarize selected posterior variables from stored draws

Summarize selected posterior variables from stored draws

## Usage

``` r
.gmap_summary(
  x,
  variables,
  probs = c(0.025, 0.5, 0.975),
  transform = NULL,
  row_names = NULL
)
```
