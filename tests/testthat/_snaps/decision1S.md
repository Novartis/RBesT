# print method works for mixed lower.tail

    Code
      print(decMixed)
    Output
      1 sample decision function
      Conditions for acceptance:
      P(theta <= 0) > 0.05
      P(theta > 0.8) > 0.5
      P(theta <= 1.2) > 0.05

